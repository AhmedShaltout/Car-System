package system;

public class Confirmation {
    private static final String Email="carrentproject2016@gmail.com";
    private static final String Password="carrentpassword";
	public static void accountConfirmation(String string) {
		// TODO Auto-generated method stub
		
	}
	public static void editConfirmation(String email2, String string) {
		// TODO Auto-generated method stub
		
	}
  
    
/*
    public static void sellConfirmation(String Email) {
        String[] to = {"",Email};
        EmailSender.sendMail(Confirmation.Email, Confirmation.Password, "Your Car has been sold.", to);
        
    }

    public static void confirmUnbookDriver(User This) {
      String Email = This.getEmail();
        String[] to = {"",Email};
        EmailSender.sendMail(Confirmation.Email, Confirmation.Password, "The Driver has been Unbooked successfully.", to);
        

    }

    public static void buyConfirmation(CarForSell BuyThisCar) {
     String Email = BuyThisCar.getConfirmationEmail();
     String[] to = {"",Email};
        EmailSender.sendMail(Confirmation.Email, Confirmation.Password, "You've successfully bought the Car.", to);
     
    }

    public static void rentConfirmation(String Email) {
        String[] to = {"",Email};
        EmailSender.sendMail(Confirmation.Email, Confirmation.Password, "You've successfully rented the Car.", to);
        

    }

    public static void editConfirmation(String Email) {
        
        String[] to = {"",Email};
        EmailSender.sendMail(Confirmation.Email, Confirmation.Password, "You've successfully edited your profile.", to);
        

    }

    public static void confirmUnbookCar(User This) {
        String Email = This.getEmail();
        String[] to = {"",Email};
        EmailSender.sendMail(Confirmation.Email, Confirmation.Password, "The Car has been successfully Unbooked .", to);

    }

    public static void confirmBookingDriver(String Email) {
        String[] to = {"",Email};
        EmailSender.sendMail(Confirmation.Email, Confirmation.Password, "You've successfully booked a driver.", to);
        

    }

    public static void accountConfirmation(User This) {
        String Email = This.getEmail();
        String[] to = {"",Email};
        EmailSender.sendMail(Confirmation.Email, Confirmation.Password, "Your Account has been created successfully.", to);

    }

    public static void forgetPasswordConfirmation(Person aThis) {
       String message = "your Password is ";
       message.concat(aThis.getPassword());
       String Email = aThis.getEmail();
       String[] to = {"",Email};
       EmailSender.sendMail(Confirmation.Email,Confirmation.Password, message , to);
       
        
    }

    public static void acceptedToSell(String confirmationEmail) {
         String[] to = {"",confirmationEmail};
        EmailSender.sendMail(Confirmation.Email,Confirmation.Password, "The car has been approved to be soled.", to);
        

    }

    public static void WaitingForAdminApproval(String Email) {
         String[] to = {"",Email};
        EmailSender.sendMail(Confirmation.Email,Confirmation.Password, "Please wait for your car to be approved.", to);

    }
    */
}
